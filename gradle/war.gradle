apply plugin: "war"

bootWar {
    mainClass = "com.hhkbdev.book.BookApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
